  <div class="container">
  <div class="title-white">
      <a class="material-icons arrow" href="javascript:history.back()">keyboard_backspace</a>
      <h1> Chats </h1>
  </div>
  <div class="all-chats-container">
  <% @trades.each do |trade| %>
    <%= link_to trade_path(trade), class: "user-name" do %>
      <%=render "trade_list_item",message:trade.messages.last %>
    <% end %>
  <% end %>
</div>
</div>
